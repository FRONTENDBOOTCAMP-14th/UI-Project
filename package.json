{
  "name": "vite-css",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "all": "run-s clean build preview"
  },
  "devDependencies": {
    "@zuminternet/sprite": "^0.0.1",
    "npm-run-all": "^4.1.5",
    "simple-svg-sprite": "^1.0.1",
    "vite": "^6.3.5"
  }
}
